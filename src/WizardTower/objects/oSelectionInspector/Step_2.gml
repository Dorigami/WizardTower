/// @description 

// make sure target exists before inspecting
if(target != noone) && (!instance_exists(target))
{
	inspect(noone);
}
if(inspect_update_script != -1) script_execute(inspect_update_script);









